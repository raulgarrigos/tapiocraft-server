const router = require("express").Router();

// GET /api/products/:productId/reviews to get a list of reviews for a specific product.
// POST /api/products/:productId/reviews to add a review to a product.
// GET /api/stores/:productId/comentarios to get a list of comments for a specific store.
// POST /api/stores/:productId/comentarios to add a comment to a store.
module.exports = router;
