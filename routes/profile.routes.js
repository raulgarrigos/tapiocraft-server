const router = require("express").Router();

// GET /api/users/profile to get the current user's profile information.
// PUT /api/users/profile to update the current user's profile information.
module.exports = router;
